// import { languageUsed, mapChineseLanguage } from '../../utils/reference/mapLanguage';
// import { arrChineseLanguage } from '../../utils/reference/mapLanguage';

// const stateGlobal = {
//   lang:'ja',
//   shopName: ''
// };

// export const createGlobalSlice = (set) => ({
//   ...stateGlobal,
//   setLanguage: (lang) => set(() => {
//     if (arrChineseLanguage.includes(lang)) {
//       return { lang: mapChineseLanguage[lang] };
//     }
//     if (languageUsed.includes(lang)) {
//       return { lang };
//     }
//     return { lang: 'en' };
//   }),
//   setShopName: (shopName) => set(() => ({ shopName }))
// });
